#use <conio>

struct Point
{
  int x;
  int y;
};

int main()
{
  struct Point *p1 = alloc(struct Point);
  p1->x = 3;
  p1->y = 4;
  print("The point is "); printint(p1->x); printint(p1->y); println(" ");
  return 0;
}
